// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: note.sql

package queries

import (
	"context"
)

const countNotes = `-- name: CountNotes :one
SELECT count(*) FROM note WHERE owner_id = $1
`

func (q *Queries) CountNotes(ctx context.Context, ownerID string) (int64, error) {
	row := q.db.QueryRow(ctx, countNotes, ownerID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createNote = `-- name: CreateNote :one
INSERT INTO note (owner_id, title, body) VALUES ($1, $2, $3) RETURNING id, owner_id, title, body, created_at
`

type CreateNoteParams struct {
	OwnerID string  `json:"owner_id"`
	Title   string  `json:"title"`
	Body    *string `json:"body"`
}

func (q *Queries) CreateNote(ctx context.Context, arg CreateNoteParams) (Note, error) {
	row := q.db.QueryRow(ctx, createNote, arg.OwnerID, arg.Title, arg.Body)
	var i Note
	err := row.Scan(
		&i.ID,
		&i.OwnerID,
		&i.Title,
		&i.Body,
		&i.CreatedAt,
	)
	return i, err
}

const deleteNote = `-- name: DeleteNote :execrows
DELETE FROM note WHERE id = $1 AND owner_id = $2
`

type DeleteNoteParams struct {
	ID      string `json:"id"`
	OwnerID string `json:"owner_id"`
}

func (q *Queries) DeleteNote(ctx context.Context, arg DeleteNoteParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteNote, arg.ID, arg.OwnerID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getNote = `-- name: GetNote :one
SELECT id, owner_id, title, body, created_at FROM note WHERE id = $1 AND owner_id = $2
`

type GetNoteParams struct {
	ID      string `json:"id"`
	OwnerID string `json:"owner_id"`
}

func (q *Queries) GetNote(ctx context.Context, arg GetNoteParams) (Note, error) {
	row := q.db.QueryRow(ctx, getNote, arg.ID, arg.OwnerID)
	var i Note
	err := row.Scan(
		&i.ID,
		&i.OwnerID,
		&i.Title,
		&i.Body,
		&i.CreatedAt,
	)
	return i, err
}

const listNotes = `-- name: ListNotes :many

SELECT id, owner_id, title, body, created_at FROM note WHERE owner_id = $1 ORDER BY created_at DESC LIMIT $2 OFFSET $3
`

type ListNotesParams struct {
	OwnerID string `json:"owner_id"`
	Limit   int32  `json:"limit"`
	Offset  int32  `json:"offset"`
}

// Queries for Note, generated by lidza gen resource and scoped to the
// owner by hand: every query takes the signed-in user's id.
func (q *Queries) ListNotes(ctx context.Context, arg ListNotesParams) ([]Note, error) {
	rows, err := q.db.Query(ctx, listNotes, arg.OwnerID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Note
	for rows.Next() {
		var i Note
		if err := rows.Scan(
			&i.ID,
			&i.OwnerID,
			&i.Title,
			&i.Body,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateNote = `-- name: UpdateNote :one
UPDATE note SET title = COALESCE($1, title), body = COALESCE($2, body)
WHERE id = $3 AND owner_id = $4 RETURNING id, owner_id, title, body, created_at
`

type UpdateNoteParams struct {
	Title   *string `json:"title"`
	Body    *string `json:"body"`
	ID      string  `json:"id"`
	OwnerID string  `json:"owner_id"`
}

func (q *Queries) UpdateNote(ctx context.Context, arg UpdateNoteParams) (Note, error) {
	row := q.db.QueryRow(ctx, updateNote,
		arg.Title,
		arg.Body,
		arg.ID,
		arg.OwnerID,
	)
	var i Note
	err := row.Scan(
		&i.ID,
		&i.OwnerID,
		&i.Title,
		&i.Body,
		&i.CreatedAt,
	)
	return i, err
}
