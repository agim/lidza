---
import Base from '../layouts/Base.astro'
---

<Base title="About">
  <h1>About</h1>
  <p>
    Every <code>.astro</code> file under <code>src/pages</code> is a page,
    built to static HTML in <code>dist/</code> and served by the Go binary.
  </p>
</Base>
